const express = require('express');
// Import and require mysql2
const mysql = require('mysql2');
const bodyParser = require('body-parser')
const app = express()
const db = mysql.createConnection({
  })
db.connect((err) => err ? console.log("error",err) : console.log("connected"))
    {
      host: 'localhost'
      // MySQL username,
      user: 'root'
      // MySQL password
      password: ''
      database: 'movie_db'
    }
    console.log(`Connected to the movie_db database.`);
  // Connect to database
  type mysql = createConnection ; /*unresolved*/ any(
  
    console.log(`Connected to the server at localhost:${}.`)
  );
  